# Jarvis PR-tool demo

Throwaway branch to exercise Controlroom's `request_changes` MCP tool
(`head=test/jarvis-pr-demo`, `base=main`). It carries one real commit so the
PR has a non-empty diff. Safe to close and delete after the demo.
